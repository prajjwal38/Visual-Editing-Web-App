# Application Settings
APP_NAME=Visual Editor API
APP_VERSION=1.0.0
DEBUG=False

# API Settings
API_V1_PREFIX=/api/v1
CORS_ORIGINS=["*"]

# Storage Settings
STORAGE_BASE_PATH=./storage

# Database Settings (for future PostgreSQL implementation)
# DATABASE_URL=postgresql://user:password@localhost:5432/video_editor

# Server Settings
HOST=0.0.0.0
PORT=8000
